query TeamSeasonRecords2020(
    $skip: Int!
    $take: Int!
    $filter: Tep2020Filter!
    $order: [Tep2020Ordering!]!
    $eventTypes: EventTypes!
    $region: Region!
    $start: DateTime
    $end: DateTime
) {
    teamRecords2020(
        skip: $skip
        take: $take
        filter: $filter
        order: $order
        eventTypes: $eventTypes
        region: $region
        start: $start
        end: $end
    ) {
        teps {
            tep {
                season
                eventCode
                teamNumber
                stats {
                    ...FullStats
                }
                team {
                    number
                    name
                }
                event {
                    ...RecordsEvent
                }
            }
            rank
            preFilterRank
        }
        offset
        count
    }
}
