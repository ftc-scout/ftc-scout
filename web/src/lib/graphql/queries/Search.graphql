query Search($searchText: String!, $numResults: Int!) {
    search(searchText: $searchText, numResults: $numResults) {
        searchText
        teams {
            searchText
            team {
                number
            }
            highlighted
        }
        events {
            searchText
            event {
                code
                season
            }
            highlighted
        }
    }
}
