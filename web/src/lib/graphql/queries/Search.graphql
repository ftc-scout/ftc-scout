query Search($searchText: String!) {
    search(searchText: $searchText) {
        searchText
        teams {
            searchText
            team {
                number
            }
            highlighted
        }
        events {
            searchText
            event {
                code
                season
            }
            highlighted
        }
    }
}
