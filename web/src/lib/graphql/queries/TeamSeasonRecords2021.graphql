query TeamSeasonRecords2021(
    $skip: Int!
    $take: Int!
    $filter: Tep2021Filter!
    $order: [Tep2021Ordering!]!
    $eventTypes: EventTypes!
    $region: Region!
    $start: DateTime
    $end: DateTime
) {
    teamRecords2021(
        skip: $skip
        take: $take
        filter: $filter
        order: $order
        eventTypes: $eventTypes
        region: $region
        start: $start
        end: $end
    ) {
        teps {
            tep {
                season
                eventCode
                teamNumber
                stats {
                    ...FullStats
                }
                team {
                    number
                    name
                }
                event {
                    ...RecordsEvent
                }
            }
            rank
            preFilterRank
        }
        offset
        count
    }
}
