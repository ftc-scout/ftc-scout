mutation Register($username: String!, $password: String!, $teamNumber: String!) {
    register(username: $username, password: $password, teamNumber: $teamNumber) {
        errors {
            field
            message
        }
        user {
            id
            username
        }
    }
}
