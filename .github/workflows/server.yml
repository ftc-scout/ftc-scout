name: Fly Deploy Server
on:
  push:
    branches:
      - main
    paths:
      - 'packages/common/**'
      - 'packages/server/**'
  workflow_dispatch:
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - run: pwd
      - run: sudo docker build . -f ${{ github.workspace }}/packages/server/Dockerfile -t ghcr.io/dragon-hatcher/ftc-scout-server:latest
      - run: sudo docker push ghcr.io/dragon-hatcher/ftc-scout-server:latest
